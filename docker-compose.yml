version: '3.9'
services:
  db:
    container_name: postgresDB
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: ${PASSWORD}
      POSTGRES_DB: ecom_db
    ports:
      - 5435:5432
